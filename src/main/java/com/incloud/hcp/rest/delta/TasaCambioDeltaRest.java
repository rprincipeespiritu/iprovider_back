/*
 * Project home: https://github.com/jaxio/celerio-angular-quickstart
 * 
 * Source code generated by Celerio, an Open Source code generator by Jaxio.
 * Documentation: http://www.jaxio.com/documentation/celerio/
 * Source code: https://github.com/jaxio/celerio/
 * Follow us on twitter: @jaxiosoft
 * This header can be customized in Celerio conf...
 * Template pack-angular:src/main/java/rest/EntitydeltaResource.java.e.vm
 */
package com.incloud.hcp.rest.delta;

import com.incloud.hcp.config.BindingErrorsResponse;
import com.incloud.hcp.domain.TasaCambio;
import com.incloud.hcp.rest.TasaCambioRest;
import com.incloud.hcp.util.DateUtils;
import com.incloud.hcp.util.Utils;
import io.swagger.annotations.ApiOperation;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.*;

import java.net.URISyntaxException;
import java.util.List;
import java.util.Optional;

import static org.springframework.http.MediaType.APPLICATION_JSON_VALUE;

@RestController
@RequestMapping("/api/tasaCambio")
public class TasaCambioDeltaRest extends TasaCambioRest {

    private final Logger log = LoggerFactory.getLogger(TasaCambioDeltaRest.class);


    @ApiOperation(value = "Devuelve lista de registros de tipo TasaCambio con la fecha de hoy", produces = "application/json")
    @PostMapping(value = "/obtenerTasaCambioActual", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<List<TasaCambio>> obtenerTasaHoy(@RequestBody TasaCambio bean, BindingResult bindingResult) throws URISyntaxException {
        BindingErrorsResponse errors = new BindingErrorsResponse();
        HttpHeaders headers = new HttpHeaders();
        if (bindingResult.hasErrors()) {
            errors.addAllErrors(bindingResult);
            throw new RuntimeException(errors.toJSON());
        }
        log.debug("Ingresando find by TasaCambio : {}", bean);
        bean.setFechaTasa(DateUtils.obtenerFechaActual());
        try {
            return Optional.of(this.tasaCambioDeltaService.find(bean)).map(l -> new ResponseEntity<>(l, HttpStatus.OK))
                    .orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            String error = Utils.obtieneMensajeErrorException(e);
            throw new RuntimeException(error);
        }
    }

    @ApiOperation(value = "Devuelve lista de registros de tipo TasaCambio en base al id de licitacion", produces = "application/json")
    @GetMapping(value = "/findLicitacion/{idLicitacion}", produces = APPLICATION_JSON_VALUE)
    public ResponseEntity<List<TasaCambio>> findLicitacion(@PathVariable Integer idLicitacion) throws URISyntaxException {
        log.debug("Ingresando findLicitacion");
        try {
            return Optional.of(this.tasaCambioDeltaService.findLicitacion(idLicitacion)).map(l -> new ResponseEntity<>(l, HttpStatus.OK))
                    .orElse(new ResponseEntity<>(HttpStatus.NO_CONTENT));
        } catch (Exception e) {
            String error = Utils.obtieneMensajeErrorException(e);
            throw new RuntimeException(error);
        }
    }


}